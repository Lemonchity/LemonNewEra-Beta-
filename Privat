local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "LemonHub|private",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Rayfield Interface Suite",
   LoadingSubtitle = "by LemonHubOffical",
   ShowText = "LemonHub", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "DarkBlue", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "LemonHub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local Tab = Window:CreateTab("Tab farm", 4483362458) -- Title, Image

local Button = Tab:CreateButton({
   Name = "Complete 1 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(0.28, 4.50, -91.91)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 2 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(0.14, 4.50, -169.41)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 3 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(0.23, 4.50, -236.52)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 4 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(0.02, 4.50, -306.36)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 5 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(-54.45, 4.50, -318.15)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 6 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(-66.78, 19.50, -306.79)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 7 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(-67.77, 19.50, -257.59)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 8 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(-66.99, 19.50, -187.48)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 9 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(-67.77, 19.50, -137.36)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 10 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(66.69, 19.50, -88.00)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 11 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(66.88, 34.50, -51.20)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 12 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(67.06, 34.50, -1.37)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})

local Button = Tab:CreateButton({
   Name = "Complete 13 stage",
   Callback = function()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local function smoothTweenToPosition(player, targetPos, duration)
    if not player.Character then return end
    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local tweenInfo = TweenInfo.new(
        duration or 2,             -- время анимации в секундах
        Enum.EasingStyle.Quad,    -- стиль сглаживания
        Enum.EasingDirection.Out  -- направление анимации
    )

    local goal = {CFrame = CFrame.new(targetPos)}
    local tween = TweenService:Create(hrp, tweenInfo, goal)

    tween:Play()

    return tween
end

-- Пример использования (локальный скрипт)

local player = Players.LocalPlayer
local targetPosition = Vector3.new(0.28, 4.50, -91.91)  -- точка назначения
smoothTweenToPosition(player, targetPosition, 3)  -- 3 секунды
   end,
})
